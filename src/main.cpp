#include <stdio.h>

#include "DataManager.h"


int main() {
    DataManager* dataManager = new DataManager();

    do {
        //something
    } while (true);


    delete dataManager;

    return 0;
}

